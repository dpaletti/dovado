target_file = "some_file_path"

task_file = "file_path" # if csv we go points mode, if json we go space mode

target_module = "some_target_module" # optional if only one module is present in file

board = "board_name"

clock_port = "clk_port_identifier"

# Optionals
implementation = false
incremental = false
synthesis_directives = ["RuntimeOptimized"]
implementation_directives = ["RuntimeOptimized"]
target_clock = 1000 # 1000 Mhz
metrics = [1, 2, 3] # default is interactive, you will be asked
